/** Default build profile name when none specified in action inputs. */
export declare const DEFAULT_PROFILE = "production";
/** Default path to the config file relative to the workspace root. */
export declare const DEFAULT_CONFIG_PATH = "./app-build.json";
/** Default Node.js version for the runner. */
export declare const DEFAULT_NODE_VERSION = "20";
/** Default Fastlane version — 'latest' installs the most recent release. */
export declare const DEFAULT_FASTLANE_VERSION = "latest";
/** Default Google Play track for Android submission. */
export declare const DEFAULT_ANDROID_TRACK = "internal";
/** Default Xcode export method for iOS builds. */
export declare const DEFAULT_IOS_EXPORT_METHOD = "app-store";
/** Default Xcode build configuration for iOS builds. */
export declare const DEFAULT_IOS_BUILD_CONFIGURATION = "Release";
/** Default Gradle build type for Android builds. */
export declare const DEFAULT_ANDROID_BUILD_TYPE = "release";
/** Whether to produce an AAB (true) or APK (false) by default. */
export declare const DEFAULT_ANDROID_AAB = true;
/** Default iOS code-signing method. */
export declare const DEFAULT_IOS_SIGNING_METHOD = "manual";
/** Default Android code-signing method. */
export declare const DEFAULT_ANDROID_SIGNING_METHOD = "manual";
/** Default source file for version information. */
export declare const DEFAULT_VERSION_SOURCE = "app.json";
//# sourceMappingURL=defaults.d.ts.map